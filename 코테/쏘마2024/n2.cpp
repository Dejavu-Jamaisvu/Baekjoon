#include <iostream>
#include <vector>

using namespace std;

int main()
{

    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n; // 네모개수
    cin >> n;

    //네모의 넘버링 구조
    //윗 줄은 1 ~ n+1
    //아랫줄 n+2 ~ 2n+2

    int number;
    //1부터 시작해서 n+1까지 의 배열을 넣을 경로
    vector<int> route;
    cin >> route.push_back(number);

    //높이가 2이므로 밑변의 크기만큼이 삼각형의 넓이가 됨을 알 수 있음.


    


    return 0;
}