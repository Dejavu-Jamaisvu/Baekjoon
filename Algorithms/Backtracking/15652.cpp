#include <iostream>
#include <vector>

using namespace std;
void Print(){

    
}
void Choose(int curr_num, int cnt){

    if(curr_num == n+1){
        if(cnt == m)
            Print();
        return;
    }

}

int main()
{
    
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    Choose(1,0);
    

    return 0;
}